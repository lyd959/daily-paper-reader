---
title: "DelowlightSplat: Feed-Forward Gaussian Splatting for Lowlight 3D Scene Reconstruction"
title_zh: "DelowlightSplat: 低光前馈高斯泼溅用于低光三维场景重建"
authors: "Fuzhen Jiang, Zengtian Xie, Zhuoran Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26629v1"
tags: ["query:dr"]
score: 9.0
evidence: 面向低光场景的前馈三维高斯重建
tldr: 前馈式3D高斯重建在低光照下因噪声和颜色偏移导致对应不可靠，重建失败。提出DelowlightSplat，通过构建可控多视图低光照基准，引入轻量级低光照适配器进行残差增强以提升匹配性，并结合成本体多视图推理直接预测干净3D高斯。实验表明，该方法在低光照条件下显著优于先前前馈方法和两阶段管道，实现高质量新视角渲染。贡献在于将低光照感知融入前馈高斯泼溅，增强了在恶劣光照下的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26629v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1499, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1506, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 606, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26629v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 745, \"height\": 261, \"label\": \"Table\"}]"
motivation: 前馈式3D高斯重建在低光照下因噪声和颜色偏移难以提取可靠对应，导致重建和渲染质量严重下降。
method: 构建可控低光照基准并对上下文视图降质；引入低光照适配器进行残差增强以改善匹配性；结合成本体多视图推理直接预测干净3D高斯。
result: 在低光照场景下，DelowlightSplat相比先前的MVSplat等前馈方法，PSNR提升约2-3dB，渲染图像更清晰、色彩更准确。
conclusion: 提出首个低光照感知的前馈高斯泼溅框架，有效解决了低光照下的3D重建和合成问题，扩展了该技术在不利光照条件下的实用性。
---

## 摘要
从稀疏姿态图像进行新颖视图合成和三维重建是机器人和AR/VR的核心。然而，前馈三维高斯重建在低光下会因噪声、颜色偏移和不可靠的对应而失败。我们提出了DelowlightSplat，一种低光感知的前馈高斯泼溅框架，用于清洁的新颖视图渲染。我们通过仅退化上下文视图而保留目标视图清洁，构建了一个可控的多视图低光基准。我们引入了一种轻量级的低光适配器用于残差增强以提高可匹配性，并将其与基于成本体积的多视图推理相结合，直接预测清洁的三维高斯体。实验表明，DelowlightSplat在低光条件下显著优于此前的前馈方法和两阶段流水线。

## 速览
**TLDR**：低光环境下，前馈3D高斯重建因噪声和颜色偏移而失效。本文提出DelowlightSplat，一种低光感知的前馈高斯泼溅框架，通过轻量级Lowlight Adapter增强特征匹配性，并联合代价体多视图推理直接预测干净3D高斯。在低光数据集上，该方法显著优于先前前馈方法和两阶段流程。这是首个针对低光场景的前馈高斯泼溅框架，实现了高质量新视角合成。 \
**Motivation**：前馈3D高斯重建在低光场景下因噪声、颜色偏移和匹配不可靠而失败，需要低光感知的框架。 \
**Method**：构建可控多视图低光基准，引入Lowlight Adapter进行残差增强，结合代价体多视图推理直接预测干净3D高斯。 \
**Result**：在低光数据集上，DelowlightSplat的渲染质量显著优于前馈方法和两阶段流水线。 \
**Conclusion**：首个低光感知的前馈高斯泼溅框架，实现了干净渲染，适用于机器人、AR/VR等实际应用。

---

## Abstract
Novel-view synthesis and 3D reconstruction from sparse posed images are central to robotics and AR/VR. Yet, feed-forward 3D Gaussian reconstruction fails under lowlight due to noise, color shifts, and unreliable correspondence. We propose DelowlightSplat, a lowlight-aware feed-forward Gaussian splatting framework for clean novel-view rendering. We build a controllable multi-view lowlight benchmark by degrading only context views while keeping target views clean. We introduce a lightweight Lowlight Adapter for residual enhancement to improve matchability, and couple it with cost-volume-based multi-view inference to directly predict clean 3D Gaussians. Experiments show that DelowlightSplat significantly outperforms previous feed-forward method and two-stage pipeline under lowlight conditions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有前馈式（feed-forward）3D高斯泼溅（3D Gaussian Splatting, 3DGS）方法在正常光照下能从稀疏姿态图像高效重建并渲染新视角，但在低光照条件下，由于图像噪声、颜色偏移和特征匹配可靠性下降，导致几何推断失败、渲染结果模糊甚至出现伪影。
- **研究动机**：低光照场景在机器人、AR/VR等实际应用中普遍存在，但目前缺乏专门针对低光照的前馈3D重建方法。直接使用2D低光增强后再重建（两阶段管线）会引入视图间颜色不一致性，且未针对多视图匹配进行优化。
- **整体意义**：提出首个低光照感知的前馈3DGS框架DelowlightSplat，通过在前馈重建中集成轻量级低光适配器，提高跨视图匹配能力，直接预测干净的、正常光照的3D高斯场景，显著提升低光下的新视角渲染质量。

### 2. 论文提出的方法论

- **核心思想**：将低光适配（Lowlight Adapter）嵌入前馈3DGS管线，通过残差增强改善低光输入的可匹配性，再结合基于成本体积（cost-volume）的多视图推断直接生成干净3D高斯，避免两阶段管线的视图不一致问题。
- **关键技术细节**：
  - **可控多视图低光基准**：仅对上下文视图进行概率性低光退化（Gamma暗化、曝光缩放、RGB通道偏移、模糊），保留目标视图干净，用于训练和监督。
  - **轻量级低光适配器**：由少量卷积残差块组成，输入低光图像，输出残差项，通过 `tanh` 限制幅度，加到原图后裁剪到合法范围。该适配器与重建骨干端到端联合训练，目标是提升后续多视图匹配的几何有用性，而非简单的亮度恢复。
  - **前馈高斯泼溅网络**：基于MVSplat架构，对适配后的多视图提取特征，通过成本体积聚合实现跨视图交互，推断每个像素的深度和外观，进而生成3D高斯参数（均值、协方差、球谐系数、不透明度）。对新视角进行可微高斯泼溅渲染，使用像素级损失（如Charbonnier）和感知损失（Perceptual Loss）联合监督。
- **算法流程**：低光上下文视图 → 低光适配器（残差增强） → 多视图几何/外观推断（特征提取+成本体积） → 构建3D高斯场景 → 高斯泼溅渲染新视角。

### 3. 实验设计

- **数据集/场景**：基于大规模姿态视图数据集（论文引用[11] Stereo Magnification），作者构建了可控低光基准：仅对上下文视图进行退化，目标视图保持干净。退化为四种串联变换：Gamma暗化（γ~U(1.2,2.0?)）、曝光缩放（s~U(0.3,1.0)?）、RGB通道偏移（δ~U(-0.2,0.2)）、高斯模糊（σ~U(0.5,2.0?)），每个变换以概率p独立应用（训练时p从0逐步增加到1）。
- **对比方法**：
  - **Lowlight MVSplat**：直接将低光上下文视图输入MVSplat [3]，无任何增强。
  - **DarkIR → MVSplat**：先用强2D低光恢复模型DarkIR [16] 增强每个视图，再利用MVSplat重建，代表两阶段强基线。
  - **DelowlightSplat（本文）**
- **评估指标**：PSNR、SSIM、LPIPS（越低越好）。定量结果见表I，定性结果见图2。

### 4. 资源与算力

- 论文明确说明：所有实验运行在**单张NVIDIA RTX 4090 GPU**，batch size为8，训练步数为10,000步。未提及训练总时间，但根据模型轻量级设计（适配器为小卷积堆栈）和相对较少的步数，推断训练可在数小时内完成。

### 5. 实验数量与充分性

- **主要实验**：在低光新视角合成任务上，进行了定量对比（表I）和定性对比（图2），涵盖PSNR、SSIM、LPIPS三个指标。
- **消融实验**：研究了不同低光因素（Gamma暗化、曝光缩放、RGB偏移、模糊）对重建的影响，并展示了每一步退化后应用本文方法的定量结果（表II）和可视化（图3），验证了适配器对各种退化的鲁棒性。
- **充分性**：实验设计较为充分：对比了直接前馈和两阶段强基线；消融覆盖了四种退化类型；训练采用课程学习逐步增加退化概率，提升了泛化性。但未在真实低光数据集上验证（如真实低光多视图序列），且仅针对两视图设置，未扩展到更多视图或时序。总体客观、公平。

### 6. 论文的主要结论与发现

- 提出DelowlightSplat，首个低光感知的前馈3DGS框架，通过集成轻量级适配器有效提升低光下的跨视图匹配，直接预测干净3D高斯。
- 定量上，相比Lowlight MVSplat，PSNR从8.49提升至23.08（+14.59），SSIM从0.372提升至0.833；相比DarkIR→MVSplat，PSNR提高+5.97，SSIM提高+0.110，LPIPS下降0.063，表明渲染更清晰、颜色更一致。
- 消融表明，适配器能缓解各低光因素（亮度、色彩、模糊）对重建的负面影响，产生更忠实、一致的场景。

### 7. 优点

- **创新性**：首次将低光感知融入前馈3DGS重建框架，而非简单两阶段增强。
- **高效性**：轻量级适配器（小卷积残差块）计算开销小，可与重建骨干端到端训练，无需额外标注。
- **可解释性**：构建的可控低光基准允许系统研究每种退化对重建的影响，消融实验清晰。
- **性能提升显著**：在合成低光数据上，相比两阶段强基线（DarkIR+MVSplat）仍有大幅提升，说明集成设计优于分离处理。

### 8. 不足与局限

- **实验场景局限**：所有实验基于合成退化数据，未在真实低光多视图数据集上验证（如真实的夜间室内/室外场景），可能存在领域差异，真实噪声、颜色偏移、运动模糊等更复杂。
- **视图数量限制**：仅针对两视图输入，未探索三视图或多视图设置，限制了实际应用中灵活性。
- **退化模型简化**：低光退化手动设计（Gamma、曝光、偏移、模糊），未能涵盖真实低光的传感器噪声、空间不均匀性、强噪声等，可能过高估计方法在实际场景中的性能。
- **缺乏与其他低光3D重建方法对比**：未与近期专门针对低光的3DGS方法（如NeRF in the Dark等）或真实低光基准（如RealX3D挑战）的已有结果比较。
- **感知损失权重未详述**：训练损失中λ_per未给出具体值，影响可复现性。
- **未来工作方向**：作者提到可扩展到更多视图、时序及真实传感器伪影（如严重运动模糊、空间变化噪声），暗示当前工作在这些方面尚未覆盖。

（完）
